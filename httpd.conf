<VirtualHost *:80>
  # Server config
  ServerName localhost
  DocumentRoot /var/www/html
  
  # Allow access to URI space
  <Directory /var/www/html>
      Allow from all
      Options -MultiViews
      # For Apache >= 2.4
      # Require all granted
  </Directory>
  
  </Directory>
</VirtualHost>